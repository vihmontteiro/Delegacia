<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatório de Usuario</title>
</head>
<body>
    <%- include("../partials/cabecalho.ejs") %>
    <div class="container">

    <% if (status == "1") { %>
        
        <div class="alert alert-success" alert-dismissible > 
            <button type="button" class="btn-close" data-bs-dismiss="alert"> </button> 
            Usuario cadastrado com <b>sucesso!</b>
        </div>
     
    <% } %>

    <% if (status == "2") { %>
        
        <div class="alert alert-danger" alert-dismissible > 
            <button type="button" class="btn-close" data-bs-dismiss="alert"> </button> 
            Usuario Removido com <b>sucesso!</b>
        </div>
     
    <% } %>
    
    
    <h2>Relatório de Usuarios</h2>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Email</th>
                <th>Nome</th>
                <th>Detalhar</th>
                <th>Atualizar</th>
                <th>Excluir</th>
            </tr>
        </thead>
        <% usuarios.forEach(usuario => { %>
            <tr>
                <td> <%= usuario.email %> </td>
                <td> <%= usuario.nome %> </td>

                <td> <a href="/usuarios/<%= usuario.email %>" class="btn btn-info" > Detalhar </a> </td>
                <td> <a href="/usuarios/<%= usuario.email %>/atualizar" class="btn btn-warning"> Atualizar </a> </td>
                <td> <a href="/usuarios/<%= usuario.email %>/remover" onclick="return confirm('Deseja realmente excluir?')" class="btn btn-danger"> Excluir </a> </td>
                
            </tr>
            
    <% }) %>

</table>



    <a href="/" class="btn btn-primary"> Voltar</a>
    <a href="/usuarios/cadastrar" class="btn btn-success">Cadastrar</a>
    </div>
    <%-include ("../partials/rodape.ejs") %>

</body>
</html>